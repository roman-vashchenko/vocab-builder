import { FC, ReactNode } from "react";
import { Route, Routes } from "react-router-dom";

const App: FC = (): ReactNode => {
  return (
    <div>
      <Routes>
        <Route></Route>
      </Routes>
    </div>
  );
};

export default App;
